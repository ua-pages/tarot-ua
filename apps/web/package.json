{
  "name": "@tarot/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build && node scripts/prerender-seo.mjs",
    "preview": "vite preview",
    "test": "cross-env TS_NODE_PROJECT=tsconfig.spec.json jasmine --config=jasmine.json"
  },
  "dependencies": {
    "vue": "^3.5.12"
  },
  "devDependencies": {
    "@types/jasmine": "^5.1.5",
    "@vitejs/plugin-vue": "^5.1.4",
    "cross-env": "^7.0.3",
    "jasmine": "^5.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "vite": "^6.0.1"
  }
}
